# Terminal Manager 需求文档

本文档记录产品需求、实现状态和验收证据。优先级从 `P0`（最高）开始。

## P0：异常关机后的登记窗口恢复

### 背景

用户登记终端窗口后，Terminal Manager 应在本地动态保存该窗口最后一次有效的工作目录。如果电脑异常关机、断电、系统崩溃或管理器来不及正常退出，下次启动 Terminal Manager 时，应自动恢复上次未正常结束的登记窗口。

### 功能要求

1. 每次登记窗口时立即保存该窗口当前工作目录。
2. 已登记窗口的工作目录发生变化时，定期更新本地记录，而不是只保存首次登记目录。
3. Terminal Manager 正常退出时写入“本次会话已正常结束”标记。
4. 启动时检测上一次管理器/系统会话是否未正常结束。
5. 仅在上一次会话异常结束时，恢复当时仍处于打开状态的登记窗口。
6. 每条记录按照最后保存的有效目录启动一个新终端窗口，并自动恢复原登记名称和窗口关联。
7. 同一条登记记录在一次恢复过程中最多启动一次，避免重复创建窗口。
8. 恢复完成后立即持久化新的窗口 ID、目录和恢复状态。
9. 单个目录恢复失败不得阻塞其他窗口恢复；失败原因应显示在管理器中并写入日志。

### 安全边界

- 只恢复已由用户明确登记的窗口，不恢复未登记窗口。
- 目录必须存在、是目录且当前用户有访问权限；无效目录不得退回到不确定路径执行。
- 第一阶段只负责“在该目录打开终端”，不得自动重放历史命令、Shell 输入、Agent 提示或其他任务。
- 正常关闭的窗口不得在下次启动时自动恢复。
- 启动时如果已经存在对应登记窗口，不得重复启动。
- 恢复状态文件应采用原子写入，并限制为当前用户可读写。

### 验收标准

- 登记窗口后切换到新目录，等待一次刷新，持久化记录中的目录同步更新。
- 模拟异常退出后重新启动管理器，原先未正常结束的登记窗口按各自目录重新打开。
- 恢复出的窗口保留原登记名称，并已与新的 X11 窗口 ID 建立关联。
- 正常关闭窗口或正常退出管理器后重新启动，不自动恢复这些窗口。
- 目录被删除或权限不足时不启动错误窗口，界面明确显示恢复失败原因。
- 连续启动两次管理器不会产生重复恢复窗口。

### 实现状态

- 已实现版本化的 `runtime-session.json` 会话快照、原子写入和 `0600` 文件权限。
- 已实现每次有效扫描更新已登记窗口目录，搜索过滤不会影响恢复快照。
- 已实现正常退出标记、异常退出检测、目录校验、逐窗口恢复和新窗口 ID 重新绑定。
- 已实现已存在窗口跳过、单次恢复去重、单项失败继续及界面/日志错误反馈。
- 恢复只打开终端目录，不执行任何历史命令。
- 自动化测试已覆盖状态读写、正常退出、目录校验、启动命令、重复恢复跳过和重新绑定。
- 已在隔离状态目录完成 X11 恢复闭环实测：按保存目录创建新终端、识别新窗口、恢复原登记名称并回写新窗口 ID；测试窗口随后已关闭。

## P0：结束窗口按钮偶发不可见

### 背景

部分界面状态下，界面底部的“移除记录”按钮会消失，导致无法稳定移除窗口登记。

### 功能要求

1. 该问题特指界面底部的“移除记录”按钮。
2. 无论终端数量、标签展开状态、窗口手工缩放、屏幕分辨率或滚动状态如何，按钮都必须保持可见且可操作。
3. 按钮不得被列表、详情区、滚动条或自适应高度逻辑挤出窗口可视区域。
4. 窗口缩小到最小允许尺寸时，主要操作区仍可访问；空间不足时应让内容区滚动，而不是隐藏操作按钮。
5. 修复不得改变“移除记录不会关闭 Shell 或终止任务”的现有安全语义。

### 验收标准

- 初始窗口、最小尺寸、最大化、手动缩放后按钮均可见。
- 新增/关闭终端以及展开/折叠多标签分组后按钮均可见。
- 后台两秒刷新不会改变按钮可见性。
- 对相应布局策略增加自动化回归测试，并在 X11 桌面进行一次实际界面验证。

### 实现状态

- 已将工作区改为三行网格布局：工具栏固定在顶部、终端列表独占中间可伸缩行、操作栏固定在底部。
- 已增加布局契约回归测试，防止列表再次挤走“移除记录”按钮。
- 已在真实 X11 桌面以最小尺寸 `860×520` 验证，“移除记录”按钮完整位于窗口可视区域。

## P1：响应式炫酷工作区与双击置顶

### 功能要求

1. 保持终端发现、登记、状态识别、恢复和高亮功能不变，升级工作区的视觉层次。
2. 主窗口最小尺寸降至原最小尺寸的四分之一，即从 `860×520` 降至 `215×130`。
3. 窗口缩小时，字体、内外间距、按钮、图标、表格行高和列宽同步缩放，最低缩放系数为 `0.25`。
4. 小尺寸下底部操作栏和“移除记录”仍然可见，内容区空间不足时使用滚动。
5. 双击终端时将目标窗口置顶；双击其他终端时，置顶跟随到新目标。
6. 普通单击、Enter 和“进入并高亮”保持原聚焦语义，不改变置顶窗口。
7. 不取消用户在管理器之外预先设置的置顶状态；管理器退出时只撤销自己添加的置顶。

### 实现状态

- 已使用新应用图标替换标题区旧 `>_` 标识，并升级深色紫青霓虹色板。
- 已实现 `1.0–0.25` 连续响应式缩放和四分之一最小窗口尺寸。
- 已实现单一受管置顶窗口、置顶转移及原有置顶状态保护。
- 自动化测试已覆盖缩放边界、EWMH 命令、置顶所有权和转移逻辑。
- 已在真实 X11 桌面验证 `215×130` 工作区完整显示，且置顶状态可真实添加和移除。
